<div class="signup-form">
	<div id="signup-errors">
		
	</div>
	<%= form_for @user, remote: true do |f| %>
	<div class="ui one column center aligned grid">
  		<div class="column six wide form-holder">
    		
    		<div class="ui form">
    		<h2 class="formtitle">Create account</h2>
      			<div class="field">
					<div class="ui left icon large input">
						<%= f.text_field :name, placeholder: "#{t('activerecord.attributes.user.name')} (required)" %>
						<i class="user icon"></i>
					</div>
				</div>
				<div class="field">
					<div class="ui left icon large input">
						<%= f.text_field :email, placeholder: "#{t('activerecord.attributes.user.email')}(required)" %>
						<i class="mail icon"></i>
					</div>
				</div>
				<div class="field">
					<div class="ui left icon large input">
						<%= f.password_field :password, placeholder: "#{t('activerecord.attributes.user.password')}(required)" %>
						<i class="lock icon"></i>
					</div>
				</div>
				<div class="field">
					<div class="ui left icon large input">
						<%= f.password_field :password_confirmation, placeholder: "#{t('activerecord.attributes.user.password_confirmation')}(required)" %>
						<i class="lock icon"></i>
					</div>
				</div>
				<%= f.submit "#{t('users.signup')}", class: "ui fluid green large button", data: { disable_with: "#{t('form.submitting')}" } %>
			</div>
		</div>
	</div>
	<% end %>
</div>